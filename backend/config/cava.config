[general]
framerate = 30.0
bars = 256

[input]
# comment this if you want your local sound output to be used as a source for the visualization
source = cava.monitor

[output]
method = raw
channels = mono
raw_target = config/cava_fifo
bit_format = 8bit

[smoothing]
integral = 50
monstercat = 0
